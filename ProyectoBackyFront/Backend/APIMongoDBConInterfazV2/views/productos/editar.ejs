<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Editar Producto</title>
</head>

<body>

    <header>
        <div class="header">
            <nav>
                <ul class="nav-links">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/productos/tabla">Productos</a>
                    </li>
                </ul>
            </nav>
            <a type="button" class="px-3 btn btn-danger" href="/logout">Logout</a>
        </div>
    </header>

    <div class="container">
        <h2>Editar Producto</h2>
        <div class="mb-5">
            <div class="row">
                <% if (product) { %>
                <!-- Formulario para la edición del producto -->
                <form action="/productos/editar/<%= product._id %>" method="POST"></form>
                    <div class="form-group col-md-6 mb-3">
                        <label for="name">Nombre</label>
                        <input type="text" name="nombre" class="form-control" id="name" value="<%= product.nombre %>" required>
                    </div>
                    <div class="form-group col-md-6 mb-3">
                        <label for="description">Descripcion</label>
                        <input type="text" name="descripcion" class="form-control" id="description" value="<%= product.descripcion %>" required>
                    </div>
                    <!-- Resto del formulario -->
                    <div class="col-lg-12 mt-5">
                        <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                    </div>
                </form>
                <% } else { %>
                <p>Error: Producto no encontrado</p>
                <% } %>
            </div>
        </div>
    </div>

</body>

</html>